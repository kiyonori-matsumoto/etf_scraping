日付,基準価格,株価,純資産総額,発行済口数
<% @dailies.each do |d| %>
<%= d.created_at.localtime.strftime("%F") %>,<%=d.base_price ? "%.2f" % d.base_price : "-" %>,<%= d.end_price ? "%.2f" % d.end_price : "-" %>,<%= "%d" % (d.total_assets || 0) %>,<%= d.total_issued ? "%d" % (d.total_issued || 0) : "-" %>
<% end %>
